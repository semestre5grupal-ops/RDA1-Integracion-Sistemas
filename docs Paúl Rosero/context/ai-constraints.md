# AI Context Constraints — {{project-name}}

- The primary runtime is {{runtime-environment}}.
- Keep {{layer-type}} handlers thin and side-effect free except {{operation-type}}.
- Place domain behavior in {{domain-module-path}} modules.
- Preserve deterministic composition in {{composition-module}}.
- Prefer explicit imports and stable module boundaries.
- Avoid hidden global state outside {{allowed-global-state}}.
